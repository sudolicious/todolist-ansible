app_dir: "/opt/todolist"

# PostgreSQL
postgres_host: "localhost"
postgres_user: "olga"
postgres_password: "1234"
postgres_db: "dbgo"
postgres_container_name: "postgres"
postgres_image: "postgres:{{ postgres_version }}"
postgres_version: "16"
postgres_port: 5432

app_repo: "https://github.com/sudolicious/todolist.git"
app_branch: "master"

# Backend
backend_code_dir: "{{ app_dir }}/backend" 
#backend_pg_user: "{{ postgres_user }}"
#backend_pg_password: "{{ postgres_password }}"
#backend_pg_db: "{{ postgres_db }}"
#backend_pg_host: "{{ postgres_container_name }}"
backend_port: 8080

# Frontend
frontend_code_dir: "{{ app_dir }}/frontend"
frontend_domain: "localhost"
frontend_api_url: "http://localhost:{{ backend_port }}"
node_version: "18.x"
frontend_port: 3000
